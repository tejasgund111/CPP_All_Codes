#include <bits/stdc++.h>
using namespace std;

/*
void bfs(unordered_map < int, set<int> &adjList, unordered_map<int, bool> &visited, int node)
{
    queue<int> q;
    vector<int> ans;
    q.push(node);
    visited[node] = 1;

    while (!q.empty())
    {
        int frontNode = q.front();
        q.pop();

        // store frontNode into ans
        ans.push_back(frontNode);

        // traverse all neighbours of frontNode
        for (auto i : adjList[frontNode])
        {
            if (!visited[i])
            {
                q.push(i);
                visited[i] = 1;
            }
        }
    }
}

*/
    int main()
    {

        return 0;
    }