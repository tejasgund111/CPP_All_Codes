#include <iostream>
#define RE(i, a, b) for (int i = a; i <= b; i++)
using namespace std;

int main()
{
    RE(i, 1, 10)
    {
        cout << i << endl;
    }
    return 0;
}

// macro is used to shorten the code and to give shortcut forms to the long codes